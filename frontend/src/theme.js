import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#162447',
      black: '#000000',
    },
    secondary: {
      main: '#1F4068',
      w: '#f0f4f8',
    },
    background: {
      default: '#162447',
    },
    accent: {
      main: '#2c5d9e',
      w: '#f0f4f8',
    },
  },
});

export default theme;
// #162447 #1F4068 #2c5d9e #f0f4f8 #ffffff
